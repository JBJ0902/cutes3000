# AUDIENCE 1 검증 기록

검증일: 2026-09-14. Node.js v24.19.0.

## 자동 검사 결과

실행: `node --check dist/app.js && node --test tests-v2/*.mjs`

기능 통합 검사: **25개 통과, 실패 0개**. UI 회귀 검사 3개를 포함한 최종 결과입니다.

- 새 시청자 모델 19개: 이름·ID 고유성, 기존 닉네임 보존, 접속 인원, 시청자별 채팅 간격, 화제성에 따른 목표 인원, 타이머 정지, 강퇴 복귀, 중복 관리 보상 제한, 차단 및 해제, 즐겨찾기 상한의 복원 몫 보전, 악플 방치, 기분과 체력, 후원 제한·회계 기록, 감사 답변 승급, 늦거나 차단된 후원 대상, 후원 TOP 20, 상황 채팅, 관심 콘텐츠 체류, 저장 유효성, 연속 진행 및 연결 지점 검증.
- 화면 생성 함수 1개: 두 채팅의 ID·본문 일치, 목록 수, 후원 강조 만료, HTML 특수문자 처리. DOM 대역을 사용한 함수 테스트이며 실제 화면 배치 검증이 아닙니다.
- 기존 엔진 검사 1개: 엔딩 경계, 졸업·재개, 저장 이관, 46일 정산·스탯 범위.
- TREND 회귀 검사: day 7(8월 23일)은 크루 합방 잠금 및 TREND 제외, day 8(8월 24일)은 체력 25·충성도 35 충족 시 활성화와 TREND 일치, 조건 미달 시 다른 콘텐츠로 대체되는지 확인했습니다.
- 최신 UI 회귀 검사: 답변 성공 시 창 닫힘, 실패 시 재입력 유지, 하단 채팅 카드 데스크톱 500px/모바일 360px, 바깥 `overflow:hidden`과 내부 단일 세로 스크롤 구조를 확인했습니다.
- 기존 UP 검사 1개: 입력 패턴별 6,000회 경쟁 시뮬레이션. 기존 난이도 알고리즘은 이번 수정에서 변경하지 않았습니다.

연속 진행 검사는 **100개 시드 × 46일 × 하루 75초 = 345,000회 시간 갱신**으로 수행했습니다. 날짜마다 저장 유효성, 최대 접속 180명, 열혈팬 20명, 후원 상한을 검사했습니다. 성과 80%의 콘텐츠 순환과 체력 부족 시 휴식을 사용했습니다. 이것은 실제 사람이 100회 플레이했다는 의미가 아닙니다.

## 추가 밸런스 비교

10개 시드씩 46일, 성과 80%, 노래·게임·쇼츠·팬 소통 순환, 체력 25 미만 시 휴식, 하루 방송 시간 75초. 악플 관리 조건에서는 10초마다 발견된 악플 시청자를 강퇴했습니다.

| 평균 결과 | 악플 무시 | 악플 관리 |
| --- | ---: | ---: |
| 최종 팔로워 | 3,081 | 3,091 |
| 최종 화제성 | 100 | 99 |
| 최종 충성도 | 100 | 100 |
| 최종 기분 | 100 | 100 |
| 별풍선 잔액 | 1,538 | 1,742 |
| 시작 열혈 명부 외 TOP 20 진입 인원 | 6 | 6 |

이는 정해진 초기 조건의 참고 실험이며 모든 플레이의 결과를 보장하지 않습니다. 악플 관리는 후원에 이득이 있지만, 기존 활동 보상이 누적되면 스탯이 100에 도달할 수 있습니다. 게임 전체의 기존 보상 곡선은 이번에 전면 수정하지 않았습니다.

## 원본 보존 검사

첨부 ZIP과 바이트 단위 비교:

- `dist/content.js`: 동일 — 수정한 대사 보존.
- `dist/core-engine.js`: 동일 — 기존 닉네임과 활동 기본 수치 보존.
- `dist/extension.css`: 동일 — 채팅 위치·너비 보존.

기존 파일 중 변경된 것은 `app.js`, `engine.js`, `gallery-ui.js`, `index.html`, `BUILD_VERSION.txt`입니다. 새 모듈 3개와 테스트·안내서를 추가했습니다.

## 미완료 검증

검증 브라우저에서 `http://localhost:8014` 접속 시 `net::ERR_BLOCKED_BY_CLIENT`가 발생했습니다. 이를 우회하지 않았으며 실제 브라우저 플레이, 모바일 터치, 시각적 겹침 및 음향 청취 검증은 **미완료**입니다. 첨부본에 이미지·음악 `assets`도 없으므로 기존 자산을 연결한 최종 실행 확인이 필요합니다. 이번 수정은 Firefox·Chrome 공통 CSS 동작을 목표로 중첩 스크롤을 제거했습니다.

실제 SOOP 서버, 실사용자, 결제, GitHub/DNS 배포를 연결하거나 시험하지 않았습니다.
