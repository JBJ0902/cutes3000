# 방셀 주문 이미지 연동

- 대표 이미지 10종은 기존 `dist/assets/gallery/aug-01.webp`~`sep-05.webp` 경로를 그대로 사용합니다.
- 주문 포즈 30종은 `dist/assets/gallery/orders/{방셀 ID}-{heart|smile|v}.webp`에 분리했습니다.
- 주문이 시작될 때 대표 이미지가 표시되고, 선택 직후 플레이어가 고른 포즈 이미지로 전환됩니다.
- 정답은 초록 테두리와 `주문 성공`, 오답은 빨간 테두리와 `요청과 다른 포즈`를 표시합니다.
- 다음 주문 전에 대표 이미지로 복귀합니다.
- 현재 방셀의 포즈 3장을 미리 로딩하며, 파생 이미지가 없거나 로딩에 실패하면 대표 이미지로 복구합니다.

## 검증

- 전체 자동 테스트 35개 통과
- 주문 이미지 30/30개 WebP 헤더·경로 확인
- 주문 이미지 30/30개 1122×1402 확인
- 로컬 HTTP 서버에서 주문 이미지 30/30개 응답 코드 200 확인
- JavaScript 구문 검사 통과
